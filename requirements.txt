annotated-types==0.7.0
anyio==4.12.1
# beautifulsoup4: parses EPUB XHTML chapters in build-time extraction
# tooling (kept out of tracked scripts). Required for pytest collection
# on CI because legacy test helpers still import it.
beautifulsoup4==4.14.3
bleach==6.1.0
certifi==2026.2.25
charset-normalizer==3.4.7
distro==1.9.0
docstring_parser==0.17.0
exceptiongroup==1.3.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jiter==0.13.0
# matplotlib: used by services/synthetic/figures/data_interp.py to
# render Data Interpretation chart PNGs. The module is imported at
# collection time by tests/synthetic/test_figures.py, so matplotlib
# must be installed on CI for pytest to even collect the suite.
matplotlib>=3.9,<4
# numpy: must allow >=2.1 for Python 3.13 wheel coverage. With the strict
# pin (numpy==2.0.2) anyone on 3.13 falls back to a source build that
# crashes on Apple clang 17. setup.sh prefers Python 3.12 anyway, but the
# loose constraint protects users who override with PYTHON=python3.13.
numpy>=2.0.2,<3
openai==2.30.0
peewee==4.0.4
# json-repair: tolerates malformed LLM JSON (unescaped inner quotes,
# missing commas, LaTeX-backslash escape mistakes). Build-time only.
json-repair>=0.40,<1
# PyMuPDF: build-time only (renders PDF pages to PNG when a question
# references a figure). Lazy-imported so the runtime app doesn't fail
# if the wheel isn't installed.
pymupdf>=1.24,<2
# pymupdf4llm: build-time only. Used by scripts/lib/marker_pipeline.py
# (Phase 4 / D3) to turn PDFs and EPUBs into per-page markdown for the
# ETS Official Guide + Big Book extraction pipelines. Pure-Python ~30 kB
# wrapper over pymupdf. Chosen over marker-pdf because marker's
# surya-ocr dep uses Python 3.10+ syntax and won't import on our 3.9.
pymupdf4llm>=0.0.27,<1
pydantic==2.12.5
pydantic_core==2.41.5
python-dotenv==1.2.1
requests==2.32.5
sniffio==1.3.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
wxPython==4.2.4
# girth: 2PL IRT MML estimator used by scripts/recalibrate_irt.py
# (Phase 4 P1). MIT, scipy-only, not a runtime dependency — the app
# never imports it. Kept pinned loose so scipy upgrades don't break it.
girth>=0.8,<1
# sympy + z3-solver: build-time only. Used by
# scripts/generate_quant_items.py (Phase 4 · D6) to symbolically verify
# that LLM-generated quant items' stated correct answer matches the
# expression the generator claims models the problem. Neither is
# imported by the runtime app; both are optional at install time
# (generate_quant_items.py degrades gracefully and refuses to run when
# sympy is missing).
sympy>=1.12,<2
z3-solver>=4.13,<5

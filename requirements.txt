annotated-types==0.7.0
anyio==4.12.1
bleach==6.1.0
certifi==2026.2.25
charset-normalizer==3.4.7
distro==1.9.0
docstring_parser==0.17.0
exceptiongroup==1.3.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jiter==0.13.0
# numpy: must allow >=2.1 for Python 3.13 wheel coverage. With the strict
# pin (numpy==2.0.2) anyone on 3.13 falls back to a source build that
# crashes on Apple clang 17. setup.sh prefers Python 3.12 anyway, but the
# loose constraint protects users who override with PYTHON=python3.13.
numpy>=2.0.2,<3
openai==2.30.0
peewee==4.0.4
# json-repair: tolerates malformed LLM JSON (unescaped inner quotes,
# missing commas, LaTeX-backslash escape mistakes). Build-time only —
# imported lazily inside scripts/extract_manhattan.py's parser.
json-repair>=0.40,<1
# PyMuPDF: build-time only (scripts/extract_manhattan.py renders PDF
# pages to PNG when a question references a figure). Lazy-imported so
# the runtime app doesn't fail if the wheel isn't installed.
pymupdf>=1.24,<2
pydantic==2.12.5
pydantic_core==2.41.5
python-dotenv==1.2.1
requests==2.32.5
sniffio==1.3.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
wxPython==4.2.4

"""
Calibration anchors shipped with every judge call.

`anchors.json` is hand-curated; rotate quarterly per the refinement
plan §11 risk-register entry on training-data leakage. Loader lives in
`services.synthetic.types.load_calibration_anchors`.
"""
